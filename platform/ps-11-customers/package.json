{
  "name": "@0815software/ps-11-customers",
  "version": "0.1.0",
  "description": "PS-11 Customers — party master data for one customer's stack: customers and the stack owner's own seller identity, with deterministic matching and per-module references. One answer to \"who is this customer?\" across every module.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "dev:api": "tsx watch server/index.ts",
    "seed": "tsx server/seed.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server/server/index.js",
    "test": "vitest run",
    "backup": "node scripts/backup.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/express": "^5.0.5",
    "@types/node": "^22.19.11",
    "@types/supertest": "^6.0.3",
    "supertest": "^7.1.4",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
